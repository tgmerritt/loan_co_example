<%= form_for(@fee) do |f| %>
  <% if @fee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fee.errors.count, "error") %> prohibited this fee from being saved:</h2>

      <ul>
      <% @fee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :origination_fee %><br>
    <%= f.text_field :origination_fee %>
  </div>
  <div class="field">
    <%= f.label :origination_fee_percent %><br>
    <%= f.text_field :origination_fee_percent %>
  </div>
  <div class="field">
    <%= f.label :maintenance_fee %><br>
    <%= f.text_field :maintenance_fee %>
  </div>
  <div class="field">
    <%= f.label :maintenance_fee_period %><br>
    <%= f.number_field :maintenance_fee_period %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
